// table de multiplication de 2
for (let table = 1; table <= 10; table++) {
	for (let i = 1; i <= 10; i++) {
		// console.log(i + " x " + table + " = ", i * table);
		console.log(`${i} x ${table} = ${i * table}`);
	}
}
